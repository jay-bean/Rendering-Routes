extends layout.pug

block content
  h1 Crags
  h2 Where will you climb today?
  div
    a(href='/crags/new')
      button Add Crag
  div(id='crag-container')
    each crag in allCrags
        div(class='crag-div')
            p= crag.name
            p= crag.location
            div(class='crag-desc-container')= crag.description
