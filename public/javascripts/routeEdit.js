const editButton = document.getElementById('route-edit-btn');
const form = document.getElementById('route-edit-form');

editButton.addEventListener('click', (e) => {
  const h1 = document.body.getElementsByTagName('h1');
  h1.color = red;
})
